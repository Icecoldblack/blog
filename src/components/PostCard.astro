---
import type { CollectionEntry } from "astro:content";

interface Props {
  post: CollectionEntry<"posts">;
}

const { post } = Astro.props;
const { title, date, description } = post.data;
const formattedDate = date.toLocaleDateString("en-US", {
  year: "numeric",
  month: "short",
  day: "numeric",
});
---

<a href={`/posts/${post.slug}`} class="post-card float-in">
  <time class="card-date" datetime={date.toISOString()}>{formattedDate.toLowerCase()}</time>
  <h2 class="card-title">{title.toLowerCase()}</h2>
  {description && <p class="card-desc">{description}</p>}
  <span class="card-arrow" aria-hidden="true">→</span>
</a>

<style>
  .post-card {
    display: flex;
    flex-direction: column;
    text-decoration: none;
    color: inherit;
    padding: 2rem;
    border: 1px solid rgba(255, 255, 255, 0.07);
    background: transparent;
    transition: border-color 0.3s ease, background 0.3s ease;
    position: relative;
  }

  .post-card::before {
    content: '';
    position: absolute;
    left: 0;
    top: 0;
    bottom: 0;
    width: 2px;
    background: rgba(147, 51, 234, 0);
    transition: background 0.3s ease;
  }

  .post-card:hover {
    border-color: rgba(255, 255, 255, 0.14);
    background: rgba(255, 255, 255, 0.02);
  }

  .post-card:hover::before {
    background: rgba(147, 51, 234, 0.7);
  }

  .card-date {
    display: block;
    font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
    font-size: 0.62rem;
    letter-spacing: 0.14em;
    color: rgba(255, 255, 255, 0.30);
    margin-bottom: 0.75rem;
    text-transform: lowercase;
  }

  .card-title {
    font-family: 'Noto Serif', Georgia, 'Times New Roman', serif;
    font-style: italic;
    font-weight: 700;
    font-size: 1.05rem;
    color: rgba(255, 255, 255, 0.88);
    line-height: 1.35;
    margin-bottom: 0.6rem;
    letter-spacing: -0.01em;
    flex: 1;
    transition: color 0.2s ease;
  }

  .post-card:hover .card-title {
    color: #fff;
  }

  .card-desc {
    font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
    font-size: 0.8rem;
    color: rgba(255, 255, 255, 0.45);
    line-height: 1.65;
    margin-bottom: 1.25rem;
  }

  .card-arrow {
    font-size: 0.8rem;
    color: rgba(255, 255, 255, 0.20);
    transition: color 0.2s ease, transform 0.2s ease;
    display: inline-block;
    align-self: flex-end;
  }

  .post-card:hover .card-arrow {
    color: rgba(147, 51, 234, 0.8);
    transform: translateX(3px);
  }

  @media (max-width: 600px) {
    .post-card {
      padding: 1.5rem;
    }
  }
</style>
